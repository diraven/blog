{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run dev server",
            "type": "shell",
            "command": "/snap/bin/hugo server --bind 0.0.0.0 --buildDrafts",
            "problemMatcher": []
        },
        {
            "label": "build",
            "type": "shell",
            "command": "rm -rf public && /snap/bin/hugo",
            "problemMatcher": []
        }
    ]
}